# Build evidence

Superseded by `agent/WORKER_REPORT.md` and `agent/PROJECT_STATE.md`.

Correction to an earlier subagent note: `java.exe` and `gradle-8.11.1-bin.zip` **do exist** (Read returns “Cannot read binary files of type exe/zip”). Glob ignores those binaries. Android `platforms/android-34` is still missing. All Shell runs in this session were rejected, so Gradle was never invoked.
